# be the hero
Desevolvido para estudo apenas, usando React e React Native.

O projeto se chama BE THE HERO e esta divido em 3 partes:
* BACKEND
* FRONTEND
* MOBILE

Para a executar acesse cada pasta pelo terminal e digite npm i, apos a instalação npm start para executar.
